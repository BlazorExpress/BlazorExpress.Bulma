<DateInput Value="@name"
           ValueExpression="() => name"
           ValueChanged="(value) => NameChanged(value)" />
<div class="mb-3">Entered name: @name</div>

@code {
    private string? name = null;

    private void NameChanged(string? value)
    {
        name = value;
        // do something
    }
}